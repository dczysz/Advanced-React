# The public facing API - What will be exposed to React

# NOT A COMMENT: import statements supposed to be commented out
# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String
    description: String
    price: Int
    image: String
    largeImage: String
  ): Item!
}

type Query {
  items: [Item]!
}
